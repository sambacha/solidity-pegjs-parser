pragma solidity >=0.5.0 <0.8.0;

contract Test {

    address public owner;
    address public _owner;
    address public _awner;
    address public _cwner;
    address public awner;

    constructor() {
        ow
    }

    function test(int number) returns (int name) {
        return number   
    }

    function test2(int number) returns (int name) {
        emit something
        emit something(1, 2);
    }

    function test3(int number) returns (int name) {
        number.t.x
        number.t.x = 10;
    }

    function test4(int number) returns (int name) {
        number = test;
        number = test

    }

    function test5(int number) returns (int name) {
        number = new test(number);
        // number = new 

    }
    
}